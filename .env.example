# Copy to .env.local and fill in values

# Backend API Gateway (campaigns, feed-posts, identity, etc. via gateway on 8080)
# Required for /campaigns list and other API calls from the browser
NEXT_PUBLIC_API_URL=http://localhost:8080
BE_API_GATEWAY_URL=http://localhost:8080

# Google OAuth - Web client ID (same as identity-service GOOGLE_CLIENT_ID)
# Create OAuth 2.0 Client ID (Web application) in Google Cloud Console
# https://console.cloud.google.com/apis/credentials
#
# Fix "Error 400: origin_mismatch": In your OAuth Client → "Authorized JavaScript origins"
# add the EXACT origin where the app runs, e.g.:
#   - http://localhost:3000
#   - http://127.0.0.1:3000
#   - https://yourdomain.com
#   - https://xxx.vercel.app
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
