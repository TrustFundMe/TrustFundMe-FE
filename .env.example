# Copy to .env and fill in your values.
# Các script run-*.ps1 (run-api-gateway, run-identity-service, run-identity-service-h2, ...)
# load .env từ thư mục gốc TrustFundMe-BE qua Load-EnvFromRoot trong common-functions.ps1.

# --- Supabase (SHARED_* được map sang SUPABASE_* khi load) ---
SHARED_SUPABASE_URL=https://your-project.supabase.co
SHARED_SUPABASE_KEY=your-service-role-or-anon-key
SHARED_SUPABASE_BUCKET=TrustFundMe
SHARED_SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# --- JWT (bắt buộc: Gateway và Identity phải dùng CÙNG giá trị; tối thiểu 32 ký tự) ---
JWT_SECRET=your-secret-key-minimum-32-characters-for-jwt-signing
JWT_EXPIRATION=86400000

# --- Database ---
IDENTITY_DB_URL=jdbc:mysql://localhost:3306/trustfundme_identity_db?createDatabaseIfNotExist=true
CAMPAIGN_DB_URL=jdbc:mysql://localhost:3306/trustfundme_campaign_db?createDatabaseIfNotExist=true
MEDIA_DB_URL=jdbc:mysql://localhost:3306/trustfundme_media_db?createDatabaseIfNotExist=true
DB_USERNAME=root
DB_PASSWORD=your-db-password

# --- Service URLs (optional) ---
IDENTITY_SERVICE_URL=http://localhost:8081
CAMPAIGN_SERVICE_URL=http://localhost:8082
MEDIA_SERVICE_URL=http://localhost:8083

# --- Email / OAuth (optional) ---
GMAIL_USERNAME=your@gmail.com
GMAIL_APP_PASSWORD=your-app-password
FRONTEND_URL=http://localhost:3000
GOOGLE_CLIENT_ID=your-google-client-id
